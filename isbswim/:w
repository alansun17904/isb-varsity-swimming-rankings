{% extends "ranker/base.html" %}
{% block body_block %}
  <h1>Rankings</h1>
  <table class="MALE">
    <thead>
      <th>Name</th>
      <th>Score</th>
    </thead>
    {% for ranking in MALE %}
    <tr>
      <td>{{ ranking }}</td> 
      <td>{{ ranking|floatformat:3 }}</td>
    </tr>
    {% endfor %}
  </table>
{% endblock %}
